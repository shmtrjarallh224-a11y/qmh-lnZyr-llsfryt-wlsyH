# Minimal Next.js + TypeScript app

This repository was converted to a minimal Next.js + TypeScript application so it can be used with tools that expect Next.js + TypeScript.

To run locally:
1. npm install
2. npm run dev
3. Open http://localhost:3000
